name: 'Setup Project'
description: 'Setup project with pnpm, Node.js, and dependencies'

runs:
  using: composite
  steps:
    - uses: pnpm/setup@5d160c5bc68a09337ad0d5654e237e03253b5879 # v1
      with:
        runtime: node@24
        cache: true
